import * as React from "react"

// @ts-ignore
import styles from "./../styles/loading-screen.module"

const LoadingScreen: React.FC = (props) => <div className={styles.root} {...props} />

export default LoadingScreen
